[project]
name = "cfm"
version = "0.1.0"
description = "Community firn model simulation with forcing data processing and gridded simulation."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []